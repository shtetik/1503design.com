%h2 Tags

%table
  %thead
    %tr
      %th Id
      %th Name
      %th Slug
      %th
        .float-right
          = link_to 'New Tag', new_admin_tag_path()
  %tbody
    - @tags.each do |tag|
      %tr
        %td= tag.id
        %td= tag.name
        %td= tag.slug
        %td
          .float-right
            = link_to 'Edit', edit_admin_tag_path(tag)
            = link_to 'Delete', admin_tag_path(tag), method: :delete, data: { confirm: "Right? The return path will not be 🤪" }