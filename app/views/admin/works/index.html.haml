%h2 Works

%table
  %thead
    %tr
      %th Id
      %th Name
      %th Has page?
      %th Display in Clients?
      %th Position
      %th
        .float-right
          = link_to 'New Work', new_admin_work_path()
  %tbody
    - @works.each do |work|
      %tr
        %td= work.id
        %td= work.name
        %td= t work.has_page?
        %td= t work.has_client?
        %td= work.position
        %td
          .float-right
            = link_to 'Edit', edit_admin_work_path(work)
            = link_to 'Delete', admin_work_path(work), method: :delete, data: { confirm: "Right? The return path will not be 🤪" }